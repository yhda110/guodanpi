module.exports = {
  devServer: {
		// port: 8080, // 端口号
		// host: 'localhost',
		// https: true, // https:{type:Boolean}
		// open: false, //配置自动启动浏览器
		// // proxy: 'http://localhost:4000' // 配置跨域处理,只有一个代理
		// proxy: {
		// 		'/toutiao': {
		// 				target: 'https://m.toutiao.com',   // 需要请求的地址
		// 				changeOrigin: true,  // 是否跨域
		// 				pathRewrite: {
		// 						'^/toutiao': '/'  // 替换target中的请求地址，也就是说，在请求的时候，url用'/proxy'代替'http://ip.taobao.com'
		// 				}
		// 		}
		// }  // 配置多个代理
	}
}